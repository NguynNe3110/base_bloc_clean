name: baseon
description: A small, understandable Flutter BLoC Clean Architecture base.
version: 1.0.0+1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  auto_route: ^10.1.0+1
  data: ^1.0.0
  domain: ^1.0.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  freezed_annotation: ^3.0.0
  get_it: ^8.0.3
  initializer: ^1.0.0
  injectable: ^2.6.0
  resources: ^1.0.0
  shared: ^1.0.0

dev_dependencies:
  auto_route_generator: ^10.2.3
  build_runner: ^2.5.4
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  freezed: ^3.0.6
  injectable_generator: ^2.9.0

flutter:
  uses-material-design: true
